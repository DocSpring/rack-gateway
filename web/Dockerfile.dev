FROM oven/bun:1.3.1-alpine AS dev

WORKDIR /app

# Copy package files
COPY package.json bun.lock ./

# Install dependencies with BuildKit cache
RUN bun install --frozen-lockfile

# Expose Vite dev server port
EXPOSE 5173

# Start dev server
CMD ["bun", "run", "dev", "--", "--host", "0.0.0.0"]
