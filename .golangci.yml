version: "2"

run:
  timeout: 1m
  modules-download-mode: readonly
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign

issues:
  max-same-issues: 0
  max-issues-per-linter: 0
