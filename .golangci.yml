version: 2

run:
  timeout: 5m
  modules-download-mode: readonly
  tests: true

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - ineffassign

issues:
  exclude-use-default: false
  max-same-issues: 0
  max-issues-per-linter: 0
